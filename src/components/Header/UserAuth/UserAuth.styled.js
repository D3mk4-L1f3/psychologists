import styled from "styled-components";

export const AuthContainerList = styled.ul`
  display: flex;
  gap: 8px;
`;

export const AuthButton = styled.button`
  all: unset;
  border: var(--border-transp-02);
  border-radius: 30px;
  padding: 14px 39px;
  font-weight: 500;
  font-size: 16px;
  line-height: 125%;
  letter-spacing: -0.01em;
  color: var(--black);
`;
