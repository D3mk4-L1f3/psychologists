import styled from 'styled-components';

export const HeaderContainer = styled.header`
  width: 100%;
  border-bottom: var(--border-transp-01);
`;

export const HeaderWrapper = styled.header`
  display: flex;
  height: 96px;
  justify-content: space-between;
  align-items: center;
`;
